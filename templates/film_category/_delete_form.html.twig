<form method="post" action="{{ path('film_category_delete', {'film_id': film_category.film_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ film_category.film_id) }}">
    <button class="btn">Delete</button>
</form>
