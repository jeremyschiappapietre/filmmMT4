{% extends 'base.html.twig' %}

{% block title %}FilmCategory{% endblock %}

{% block body %}
    <h1>FilmCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Film_id</th>
                <td>{{ film_category.filmId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('film_category_index') }}">back to list</a>

    <a href="{{ path('film_category_edit', {'film_id': film_category.film_id}) }}">edit</a>

    {{ include('film_category/_delete_form.html.twig') }}
{% endblock %}
